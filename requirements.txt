# ---- Core (needed from the start) ----
openaI                # LLM for quizzes, adaptation, diagrams (optional)
google-genai          # Gemini SVG comic generation
python-dotenv         # load API keys from .env
pypdf                 # read text from PDFs
chromadb              # vector store for RAG
gTTS                  # text-to-speech (audio mode)

# ---- RAG helpers ----
numpy                 # numeric ops used by embeddings/vector store

# ---- Testing ----
pytest                # run the tests/ folder

# ---- Better PDF extraction (optional) ----
pdfplumber            # use if pypdf struggles with columns/tables

# ---- Audio processing (optional) ----
faster-whisper        # transcribe lecture audio to text

# ---- Audio quality (optional) ----
edge-tts              # better/free narration if gTTS isn't enough
# ---- Web server support ----
Flask                # lightweight backend server for the frontend
flask-cors           # allow frontend requests to the API
# ---- Frontend (add when you build the UI) ----
streamlit             # web frontend (renders Mermaid + SVG in browser)
