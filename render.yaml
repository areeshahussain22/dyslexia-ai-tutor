services:
  - type: web
    name: dyslexia-ai-tutor
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: PORT
        value: "8080"
      - key: OPENAI_API_KEY
        sync: false
    healthCheckPath: /api/health
